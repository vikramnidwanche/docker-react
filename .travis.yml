sudo: required
language: generic 
services:
  - docker

before_install:
  - docker build -t vikramnidwanche/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true vikramnidwanche/docker-react npm run test -- --coverage
